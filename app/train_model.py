import pandas as pd
import gzip
import os

#Should take a csv.gz, take the first 1 000 000 lines and train a model, and put it in models/{set} or if the folder doesnt exist create it

# How many rows to keep (set None to keep all)
MAX_ROWS = 100_000

def main():
    return 0

if __name__ == "__main__":
    main()
